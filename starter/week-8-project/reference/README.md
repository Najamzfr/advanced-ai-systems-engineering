# Week 8 reference solution

The reference release contains the final manifest, failure matrix, monitored dashboard and a concise technical defense.

Build the starter from a clean directory, run the smoke release and inspect the report fields before deploying anything. Run `make check-step-1`; the capstone starts only when the release contract is visible.

The smoke path is a wiring check. The real-data target is: Reference target: 150+ release cases, evaluation/security/observability checks, injected failures and a reproducible deployment report.
